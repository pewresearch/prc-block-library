/**
 * Utilities for managing core/table data
 */

const constructTableRow = () => {};

const constructTableCell = () => {};

export const parseJSONToAttributes = data => {
    console.log(data);

    const thData = data[0];
    data.shift();
};

export const test = data => {};
